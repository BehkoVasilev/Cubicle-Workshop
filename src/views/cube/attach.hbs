<main>
  <h1>Attach a new accessory</h1>
  <div class="form">
    <h2>{{cube.name}}</h2>
    <img class="cube" src="{{cube.imageUrl}}">
    <form method="POST">
      <label for="accessory">Accessories</label>
      {{#if accessories}}
        <select id="accessory" name="accessory" />
          {{#each accessories}}
            <option value="{{this._id}}">{{this.name}}</option>
          {{/each}}
        </select>
      {{else}}
        <h3 class="italic">This cube has all available accessories so far or there is no available at all...
        </h3>
      {{/if}}
      {{!-- {{#each accessories}}
      <option value="{{this._id}}">{{this.name}}</option>
      {{else}}
      {{#if cube.accessories}}
      <option>
        <h3 class="italic">This cube has all available accessories...
        </h3>
      </option>
      {{else}}
      <option>
        <h3 class="italic">there is no available...
        </h3>
      </option>
      {{/if}}
      {{/each}} --}}
      <input type="submit" value="Attach">
    </form>
    <a class="btn" href="/">Back</a>
  </div>
</main>